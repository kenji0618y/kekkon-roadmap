import { readFileSync, writeFileSync } from "node:fs";
import { join, dirname } from "node:path";
import { fileURLToPath } from "node:url";

const root = join(dirname(fileURLToPath(import.meta.url)), "..");
const research = join(root, "..", "marriage-research");

const stampsPath = join(root, "src/data/stamps.json");
const homePath = join(root, "src/data/home.json");

const stamps = JSON.parse(readFileSync(stampsPath, "utf8"));
const byId = Object.fromEntries(stamps.map((s) => [s.id, s]));

function setFaq0(s, q, a) {
  if (!Array.isArray(s.faq) || s.faq.length === 0) s.faq = [{ q, a }];
  else s.faq[0] = { q, a };
}

const before = {};
function capture(id) {
  const s = byId[id];
  if (!s) throw new Error("missing " + id);
  before[id] = { why: s.why, miss: s.miss };
}

const top30 = [
  "A必1","A必5","A必3","A必6","A必7","A必2","A必4","D1","D2","D4",
  "D3","C即5","C即6","B7","D15","D8","G9","G7","G8","G11",
  "G14","G20","G21","G23","G15","G19","F5","F2","F13","B2",
];
for (const id of top30) capture(id);

// ——— Top 30 rewrites ———

{
  const s = byId["A必1"];
  s.why = "式なし届出婚でも、祝金・休暇・住宅の起算が挙式なら0。両社の規程PDFが一次。口頭の「出ます」は信用しない。";
  s.steps = [
    "双方の就業規則・慶弔・賃金・社宅規程をPDF保存する（届出前）",
    "祝金・特別休暇の起算（入籍／挙式）と申請期限を蛍光ペンで印す",
    "人事に「式なし・婚姻届のみで対象か」をメール一文→返信保管",
    "必要書類（受理証明350円で足りるか／戸籍か）をリスト化",
    "D1・D2・D4の申請カレンダーに期限を転記",
  ];
  setFaq0(s, "式をしないと祝金は必ず0？", "挙式必須・挙式起算なら0。入籍起算なら式なしでも候補。額は会社差・要確認。規程の数字だけ使う。");
}

{
  const s = byId["A必5"];
  s.why = "住居は通勤数学。2人広島駅通勤なら候補の芯は南区・安佐北・廿日市。東広島・呉は定期が家賃差を食う。補助金で市を選ぶな。";
  s.steps = [
    "双方の勤務駅と希望区を書き、駅探で定期代を出す",
    "中区掲載家賃との差−二人定期で月次を出す（掲載≠契約）",
    "東広島・呉・安佐南は「補助金目的」で選ばない（会社が定期全額なら再判定）",
    "引越は通常期2人・15km未満約9.1万／50km未満約10万の市場例。敷礼仲は物件で一次（広島平均は要確認）",
  ];
  setFaq0(s, "結婚新生活のために市外へ？", "広島市は未導入0円。他市も所得合計500万未満に届きにくい。ジョブロックとも矛盾。賞品に出さない。");
}

{
  const s = byId["A必3"];
  s.why = "改氏する側だけ名義が連鎖する。棚卸しなしは給与振込全額戻し、車15日、不動産2年を同時に踏む。";
  s.steps = [
    "改氏側の名義一覧をシート化（銀行・証券/NISA・保険・クレカ・携帯・免許・旅券・車・不動産・資格・勤務先・印鑑）",
    "期限を印す：転居14日／車15日／不動産変更登記2年",
    "給与振込口座の名義変更タイミングを人事・給与に確認",
    "戸籍全部事項の通数とコンビニ交付条件（本籍広島市等）を確定",
  ];
  setFaq0(s, "いちばん痛いミスは？", "給与振込名義不一致で全額戻ること。次点は車15日・不動産2年の失念。");
}

{
  const s = byId["A必6"];
  s.why = "共働き800万超は扶養の壁の上。被扶養に落とすと傷病手当・出産手当・厚生年金の報酬比例が消える。双方第2号のまま固定。";
  s.steps = [
    "「双方とも各自の健保・厚生年金（第2号）」を文書で固定",
    "130万・同居相手の1/2未満は今の二人に非該当と共有",
    "人事に「結婚しても扶養異動は出さない」を確認",
    "育休・時短で壁を跨ぐときだけ再判定メモ",
  ];
  setFaq0(s, "保険料が安くなるのでは？", "短期の保険料減より、傷病・出産手当と将来の報酬比例喪失の方が大きいことが多い。扶養は得ではない。");
}

{
  const s = byId["A必7"];
  s.why = "税の医療費控除（生計一で合算可・足切り10万）と保険の高額療養（同一保険者のみ）は別物。別会社健保は高額合算オフ。混ぜると窓口か還付のどちらかを落とす。";
  s.steps = [
    "「医療費控除＝税／高額療養＝健保」を二人で固定",
    "各自の健保で限度額認定またはマイナ保険証の区分を確認",
    "入院が分かった時点で認定申請",
    "領収は世帯保管。補填を差し引いて税側を計算",
  ];
  setFaq0(s, "夫婦で高額療養を合算できる？", "別会社健保同士では原則できない。各自の上限だけ。");
}

{
  const s = byId["A必2"];
  s.why = "法律婚は同じ氏が必須。届書に書く氏が改氏カスケードの起点。後から変えるのは家裁等で重い。";
  s.steps = [
    "別氏の法律婚はできないことを二人で共有",
    "称する氏を決め、届書欄の文言を確定",
    "改氏側の後続リスト量（A必3）を先に見る",
    "通称は社内ルール次第（法律上の氏とは別）と勤務先確認",
  ];
  setFaq0(s, "旧姓のまま法律婚できる？", "できない。通称は勤務先・銀行の運用次第。");
}

{
  const s = byId["A必4"];
  s.why = "マイナと基礎年金番号が結合済みなら、事業主経由の氏名変更届は原則不要。届出前に確認し、未結合だけ残作業にする。";
  // steps keep structure, polish last
  s.steps = [
    "ねんきんネットまたは勤務先人事に「マイナと基礎年金番号の結合済みか」を確認する",
    "未結合なら結合手続の要否と担当をメモする",
    "健保組合・協会けんぽ側の氏名変更ルートも併せて確認する",
    "確認結果をA必3シートの「年金・健保」行へ書き込む",
  ];
  setFaq0(s, "結合済みなら何もしなくてよい？", "原則氏名変更届は不要。健保のみ等の例外は人事に一言。");
}

{
  const s = byId["D1"];
  s.why = "祝金は慶弔規程の起算と期限が本体。挙式必須なら式なし0。双方重複可が多い。額は規程以外書かない（A必1のコピーが先）。";
  s.steps = [
    "A必1で保存した慶弔規程の祝金条項を開く",
    "起算・申請期限・必要書類を抽出",
    "期限内申請→明細で着金確認までクローズしない",
    "配偶者側会社も同順で実施",
  ];
  setFaq0(s, "いくらもらえる？", "会社差・要確認。民間最頻2〜3万台/人は参考であり実額ではない。市の結婚新生活とも別物。");
}

{
  const s = byId["D2"];
  s.why = "結婚休暇は特別休暇。式なしは入籍起算なら取れる。挙式前後型なら0で年休を削る。就業規則のコピーが一次（A必1）。";
  s.miss = "挙式型のまま式なし→休暇0／年休消費";
  s.steps = [
    "A必1で保存した就業規則の日数・分割・連続・土日扱いを読む",
    "起算と取得期限をカレンダーへ",
    "上長承認ワークフローを通す",
    "挙式型のみなら年休温存計画に切替",
  ];
  setFaq0(s, "式をしないと取れない？", "挙式前後型だと使えない。入籍起算なら可。会社差。");
}

{
  const s = byId["D4"];
  s.why = "住宅手当・社宅は規程次第で最大の得にも損失にもなる。退寮期限・世帯主のみ・持ち家除外を先に読む。手当が増えない転居はするな。";
  s.steps = [
    "A必1の社宅・賃金規程を開く",
    "結婚後の手当増減・退寮期限を抽出する",
    "転居前に手当シミュレーションする",
    "必要書類（住民票・婚姻証明）を揃える",
  ];
  setFaq0(s, "平均月1.87万は自分も？", "支給された人の全国型。個人円ではない。規程の額だけ。");
}

{
  const s = byId["D3"];
  s.why = "家族手当に103万／130万制限があるなら共働き800万超は0候補。制限なしの「婚姻したら出る」型だけ残る。期待値を先に切れ。";
  s.steps = [
    "A必1で保存した賃金規程の家族手当・扶養手当条項を開く",
    "収入制限の有無と金額を確認する",
    "制限ありなら0と割り切り期待しない",
    "制限なしなら事実発生月の申請漏れを防ぐ",
  ];
  setFaq0(s, "平均月1.76万は？", "支給された人の全国平均。個人円ではない。制限つきなら既定0。");
}

{
  const s = byId["C即5"];
  s.why = "人事への結婚申告と祝金申請は規程の日数勝負。期限切れは0。双方重複可が多い。A必1→B7の確認後に即申請。";
  s.steps = [
    "A必1/B7で確定した期限日内に申告する",
    "必要書類（受理証明350円等）を添付する",
    "振込月と明細を確認するまでクローズしない",
    "配偶者側の会社も同様に申請する",
  ];
  setFaq0(s, "式なしでも出る？", "入籍起算なら候補。挙式必須なら0。市の結婚新生活とは別物・賞品にしない。");
}

{
  const s = byId["C即6"];
  s.why = "結婚休暇の取得は起算と期限のオペ。式なしは入籍起算なら取る。挙式前後型は使えない。平均労働日5.2は参考（会社差）。";
  s.steps = [
    "A必1で保存した就業規則で日数・連続/分割・土日の扱いを読む",
    "起算日と取得期限（届出後○か月等）を確認する",
    "上長・人事に取得届を出す",
    "取れない型なら年休温存計画を立てる",
  ];
  setFaq0(s, "式なしだと取れない？", "挙式前後型は不可。入籍起算なら可。規程を読め。");
}

{
  const s = byId["B7"];
  s.why = "届出前に人事へ、慶弔の起算・申請期限とマイナ×年金結合をメール確認する。挙式必須なら式なし祝金0。返信が一次。";
  setFaq0(s, "Leanでも確認する？", "必須。起算が挙式だと0になる。");
}

{
  const s = byId["D15"];
  s.why = "高所得は雇用保険給付が上限で頭打ち。会社の育休上乗せと男性育休実績は産む前に規程で読め。";
  s.steps = [
    "A必1で保存した育児休業規程と給付上乗せを読む",
    "両立支援のひろば等で男性取得実績を見る",
    "雇用保険の上限（67%期間・50%期間）を把握する",
    "出生後休業支援（13%加算等）の条件をメモする",
  ];
  setFaq0(s, "給付上限の例は？", "支給日数30日・R8.8.1以後で67%期間上限332,454／50%期間248,100等。個人標報は invent しない。");
}

{
  const s = byId["D8"];
  s.why = "団体生命・GLTDの受取人が親のままだと、法律婚の実益が死亡時に死ぬ。配偶者へ変更。保障内容は会社差・要確認。";
  s.miss = "親のまま死亡→配偶者に渡らない";
  s.steps = [
    "A必1関連の団体保険の加入有無と現行受取人を確認",
    "中途変更か年1回募集かを把握",
    "受取人を配偶者へ変更し控えを保管",
    "個人生命との重複を整理",
  ];
  setFaq0(s, "会社負担の平均は？", "保険料の個人平均ではない。保障内容と受取人を見る。会社差・要確認。");
}

{
  const s = byId["G9"];
  s.why = "妊婦歯科は委託歯科で健診0円（市案内）。出産後は対象外。治療費は保険自己負担。";
  s.miss = "出産後は対象外。健診を逃すと0円枠が消える";
  s.steps = [
    "母子手帳・市案内で受診券を確認",
    "委託歯科を選び予約",
    "出産までに1回受診",
    "追加治療は自己負担と了解",
  ];
  setFaq0(s, "完全無料？", "健診は0円案内。治療は別。出産後は対象外。");
}

{
  const s = byId["G7"];
  s.why = "妊婦支援給付金1回目は5万円。心拍確認後に申請。時効は心拍確認日から2年。住民票が市外だと市の給付が死ぬ。";
  s.miss = "時効2年超過・住民票市外で0";
  s.steps = [
    "市案内で対象要件を一次確認（創作しない）",
    "心拍確認日＋2年をカレンダー登録",
    "口座は妊婦本人で申請",
    "入金確認までクローズしない",
  ];
  setFaq0(s, "高所得でも出る？", "所得制限の記載なし（市案内）。住民票と時効が本体。");
}

{
  const s = byId["G8"];
  s.why = "2回目は胎児1人あたり5万円。申請開始は予定日の8週前から。時効2年。流産・死産も条件付きで市FAQ一次。";
  s.miss = "時効超過で0。流産・死産は条件付き申請を逃すな";
  s.steps = [
    "2回目の申請開始時期（予定日8週前）を確認する",
    "胎児数に応じた額を確認する",
    "時効（予定日8週前から2年）をカレンダー登録する",
    "流産・死産時の条件を市FAQで確認する",
  ];
  setFaq0(s, "金額は？", "胎児1人50,000円。単胎なら1回目と合わせて10万。");
}

{
  const s = byId["G11"];
  s.why = "出産育児一時金は産科医療補償対象で50万/児（対象外・22週未満は48.8万）。直接支払が既定。国保と二重取り不可。時効2年。";
  s.steps = [
    "産院と健保で直接支払／受取代理を確認",
    "差額が出る場合の事後申請を忘れない",
    "国保・健保の資格を二重取りにならないよう確認",
    "出産日翌日から2年の時効を意識",
  ];
  setFaq0(s, "窓口で50万現金？", "多くは直接支払で産院へ。差額精算の型を産院で確認。");
}

{
  const s = byId["G14"];
  s.why = "児童手当は出生日等の翌日から15日以内。遅れはその月分から落ちる。3歳未満第1子月15,000／以降10,000。所得制限なし。";
  s.steps = [
    "出生届と同日〜翌営業日で認定請求",
    "起算（出生日等の翌日）から15日をカレンダー登録",
    "必要書類を区へ提出する",
    "振込月・額を通知で確認する",
  ];
  setFaq0(s, "高所得は対象外？", "所得制限なし（R6.10分〜）。落とすのは15日超過。");
}

{
  const s = byId["G20"];
  s.why = "三世代近居は引越し等の1/2・上限10万。子（出産予定含む）＋転居前申請＋親が広島市内住民登録。子なし今は対象外。所得制限なし。";
  s.miss = "転居後申請・子なし・親市外・先着枠切れで0";
  s.steps = [
    "親世帯が広島市内に住民登録しているか確認する（廿日市・東広島・呉のみは対象外）",
    "子（出産予定含む）の要件と、住替え前に電子申請等する",
    "近居定義（同一小学校区または直線1.2km以内）で物件を選ぶ",
    "町内会加入意思・他助成との重複禁止を満たし、先着枠（R8・130件・要確認）を確認する",
  ];
  setFaq0(s, "800万超でもらえる？", "所得では落ちない。落とすのは子なし・転居後申請・親が市外。");
}

{
  const s = byId["G21"];
  s.why = "団地空き家リフォームは工事費1/2・上限50万/戸（合計20万以上）。空き家活用計画の対象戸＋子育て世帯。子なし今は対象外。受付の開閉は市一次だけを見る（口コミの終了と混ぜるな）。";
  s.miss = "子なし今は対象外。受付開閉は市一次・要確認（口コミで終了と教えるな）";
  s.steps = [
    "市ページでR8受付の開閉・残を一次確認（要確認）",
    "計画対象の団地戸建か確認",
    "子世帯要件を確認",
    "見積が20万以上か確認してから動く",
  ];
  setFaq0(s, "もう終了？", "口コミで終了と教えるな。市の受付ページが一次。子なし今は対象外。");
}

{
  const s = byId["G23"];
  s.why = "0–2歳認可保育料は高所得ほど最高階層に張り付く。無償化は非課税のみ→800万超はNO。3–5歳クラスの保育料無償と混ぜるな。";
  s.miss = "0–2歳無償化を当てにすると毎月の私費が裸で残る";
  s.steps = [
    "区の階層表と税額通知の所得割で自己負担を確認（額の断定禁止・要確認）",
    "申込スケジュールを区保育課で確認",
    "親近居（G20）と病児の実務を並行",
    "延長保育は無償化対象外と了解",
  ];
  setFaq0(s, "0–2歳も無償？", "既定の高所得では対象外（exclude）。C17なら標準時間月62,400の例あり——階層は要確認。");
}

{
  const s = byId["G15"];
  s.why = "こども医療はR8.12まで所得制限あり（0人扶養532万）。R9.1診療分から所得制限なし。2026年末出生で制限に当たる型は年末まで0になりやすい。";
  setFaq0(s, "いつから制限なし？", "R9.1〜。それまでは所得で落ちる型がある。");
}

{
  const s = byId["G19"];
  s.why = "1か月児・乳児一般健診は案内票で受ける。1回あたり限度円は市HTML一次未掲載→要確認。窓口で自己負担を聞け。";
  s.miss = "限度円を invent して家計計画に入れない";
  setFaq0(s, "費用は？", "限度円は一次未掲載→要確認。創作しない。");
}

{
  const s = byId["F5"];
  s.why = "先進的窓リノベ2026は戸上限100万。申請は登録事業者経由、予算で即終了（目安〜2026-12-31）。ドアのみ不可。既存改修向け。";
  s.miss = "予算切れ・ドアのみ申請で0";
  s.steps = [
    "対象が窓工事か確認（ドアのみ不可）",
    "登録事業者経由で予約・申請",
    "予算残を確認（即終了あり）",
    "他補助との併用ルールを読む",
  ];
  setFaq0(s, "上限は？", "戸100万。最低申請額等は公式一次。予算切れあり。");
}

{
  const s = byId["F2"];
  s.why = "みらいエコ2026・GXは戸あたり目安110万（5〜8地域）。広島の区分は要確認。予算で即終了。基礎工事は2025-11-28以後着手など公式要件を契約で満たす。";
  s.steps = [
    "広島の地域区分を公式で確認する（要確認）",
    "予算残・予約状況を事業者経由で確認する",
    "着手日要件（2025-11-28以後）を契約書で満たす",
    "他補助との併用可否を公式要件表で読む",
  ];
  setFaq0(s, "110万は確定？", "5〜8地域目安。1〜4は125万等。広島区分は要確認。");
}

{
  const s = byId["F13"];
  s.why = "「子育てエコホーム」は旧事業名。申請するな。後継はみらいエコ／窓／給湯（F2–F6）。旧チラシの金額で動かない。";
  s.steps = [
    "旧チラシ・旧事業名での申請をしない",
    "後継（みらいエコ等 F2–F6）のページへ切り替える",
    "事業者にも旧名で出さないよう伝える",
    "古い記事・チラシに引っ張られない",
  ];
  setFaq0(s, "まだ使える？", "旧名では申請するな。後継事業だけ見る。");
}

{
  const s = byId["B2"];
  s.why = "夜間・休日はお預かり審査。不備なら再来庁で婚姻日がずれる。式なしLeanは平日に区市民課でプレチェックしてその日をM0にする方が安い。本庁舎では出さない。";
  s.steps = [
    "区市民課で届書を入手（時間外でも用紙は渡る／説明は平日）",
    "平日に記入内容・消えるペン・証人欄を職員と潰す",
    "不備ゼロで提出し提出日＝婚姻日を確定",
    "住所変更が必要なら別手続・14日・夜間不可を同時に確認",
  ];
  setFaq0(s, "夜間でも当日受理？", "お預かり後に審査。不備なら再来庁で婚姻日がずれる。");
}

// ——— Light pass: meta language, miss nulls, company CTA ———

{
  const s = byId["C他6"];
  s.why = "結婚・子育て資金一括贈与の制度窓は〜2027-03-31。中身はA得11と同じ。期限カレンダー用の印。";
}

{
  const s = byId["C即4"];
  s.why = "住民票の写しは会社・各種届出の添付用。1通300円。必要枚数と記載事項は提出先で先に確定。";
  s.miss = "枚数・記載不足で再取得（300円×追加）";
  if (!s.faq?.length) setFaq0(s, "いつ何枚？", "提出先のチェックリストが一次。本籍・続柄の要否を先に聞け。");
  else setFaq0(s, "いつ何枚？", "提出先のチェックリストが一次。本籍・続柄の要否を先に聞け。");
}

{
  const s = byId["G18"];
  s.miss = "採血料自己負担を見落とすと自己負担が裸で残る";
}

{
  const s = byId["A無5"];
  s.miss = "無料相談枠を使わず有料相談だけに流れる";
}

// Company CTA light pass
const companyCta = ["D6","D7","D9","D13","D14","C他7","D5","D10","D12"];
for (const id of companyCta) {
  const s = byId[id];
  if (!s) continue;
  if (!/A必1|規程|会社差|要確認/.test(s.why)) {
    s.why = s.why.replace(/。$/, "") + "。会社差・要確認（A必1の規程コピーが一次）。";
  }
  if (s.steps?.[0] && !/A必1/.test(s.steps[0]) && !/規程/.test(s.steps[0])) {
    s.steps[0] = "A必1で保存した該当規程を開く／" + s.steps[0];
  }
}

{
  const s = byId["D5"];
  s.miss = "家賃比較に使済みの通勤手当を得に足すと二重計上";
}

{
  const s = byId["D6"];
  s.why = "健保被扶養は失敗が既定。各自第2号のまま（A必6正本）。会社差・要確認。";
  s.miss = "誤って被扶養申請→傷病・出産手当と報酬比例が消える";
}

{
  const s = byId["C他7"];
  s.why = "健保被扶養の届出は事実から5日以内に事業主、が多い。既定共働きは非該当（A必6）。誤適用が失敗。会社差・要確認。";
}

{
  const s = byId["E3"];
  s.why = "ソフトバンク家族割引はプラン公式表が一次。届出前の別居は詰まりやすい。公式表以外の円は invent しない。";
}

{
  const s = byId["E5"];
  s.why = "スマホ＋光セット割は金額がプラン公式表。乗換違約金を差し引かない比較はしない。公式以外 invent しない。";
}

{
  const s = byId["C他3"];
  s.why = "NISA年間枠は暦年末で消滅（繰越なし）。360万×2の機会損失を防ぐための期限印。思想の正本はA得1。";
}

// Write stamps
const stampsOut = JSON.stringify(stamps, null, 2) + "\n";
writeFileSync(stampsPath, stampsOut);
writeFileSync(join(research, "app-seed/stamps-v2.json"), stampsOut);

// ——— home.json ———
const home = JSON.parse(readFileSync(homePath, "utf8"));
home.headline = "式は切る。届出は出す。現金は会社規程とNISA二人枠だけ見ろ。";
home.anti_lie_banner =
  "結婚新生活＝0。配偶者控除＝既定0。高額療養≠医療費控除。マイナ90日＝転入のみ。";
home.tomorrow_3_actions = [
  {
    id: "t1",
    stamp_id: "A必1",
    title: "両社の慶弔・就業規則をPDF保存",
    detail: "起算＝入籍か挙式か、期限は何日かを一文で確定（A必1）",
  },
  {
    id: "t2",
    stamp_ids: ["A必2", "A必3"],
    title: "称する氏を決める。改氏側だけ名義棚卸し",
    detail: "口座・NISA・保険・免許・車を棚卸し（A必2・A必3）",
  },
  {
    id: "t3",
    stamp_id: "B2",
    title: "平日に区市民課で婚姻届プレチェック",
    detail: "夜間はお預かり審査。届出0円（B2）",
  },
];
const homeOut = JSON.stringify(home, null, 2) + "\n";
writeFileSync(homePath, homeOut);
writeFileSync(join(research, "app-seed/home.json"), homeOut);

// Summary for report
const summary = top30.map((id) => {
  const s = byId[id];
  const b = before[id];
  return {
    id,
    before_why: b.why,
    after_why: s.why,
    before_miss: b.miss,
    after_miss: s.miss,
  };
});
writeFileSync(
  join(research, "_ceo-copyedit-diff.json"),
  JSON.stringify(summary, null, 2) + "\n",
);
console.log("OK top30=" + top30.length + " stamps=" + stamps.length);
console.log("headline:", home.headline);
console.log("banner:", home.anti_lie_banner);
